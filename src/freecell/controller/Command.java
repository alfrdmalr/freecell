package freecell.controller;

/**
 * Represent the sequence of a command in a game of Freecell. A valid command has
 * three parts to it: the source, the card, and the destination information.
 */
public enum Command {
  SOURCE, CARD, DEST
}
